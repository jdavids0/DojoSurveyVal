@model Survey

<div class="container mt-5">
        <div class="d-flex flex-column justify-content-center border border-dark w-50">
            <div class="mt-2 mb-2">
                <h4 class="text-center"><u>Submitted Info</u></h4>
            </div>
            <div class="d-flex justify-content-around">
                <div>
                    <p>Name:</p>
                    <p>Language:</p>
                    <p>Location:</p>
                    <p>Comment:</p>
                </div>
                <div>
                    <p>@Model.Name</p>
                    <p>@Model.Language</p>
                    <p>@Model.Location</p>
                    <p>@Model.Comment</p>
                </div>
            </div>
            <div class="d-flex justify-content-end mb-3 mr-3">
                <a href="/" class="btn btn-info btn-outline-dark">Go Back</a>
            </div>